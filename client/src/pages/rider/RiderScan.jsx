/**
 * /rider/scan — placeholder page (Task D1.2)
 * Stub created by Developer 1 for routing purposes only.
 * Owner of real content: Developer 3 (see D3.5 in the implementation plan) —
 * this is where QRScanner (html5-qrcode) will be integrated.
 */
export default function RiderScan() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Scan QR Code</h1>
      <p className="mt-2 text-gray-500">Route: /rider/scan — placeholder (owned by Developer 3)</p>
    </div>
  );
}

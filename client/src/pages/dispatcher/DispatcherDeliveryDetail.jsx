/**
 * /dispatcher/deliveries/:id — placeholder page (Task D1.2)
 * Stub created by Developer 1 for routing purposes only.
 * Owner of real content: Developer 2 (see D2.12 in the implementation plan).
 */
export default function DispatcherDeliveryDetail() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Delivery Detail (Dispatcher view)</h1>
      <p className="mt-2 text-gray-500">Route: /dispatcher/deliveries/:id — placeholder (owned by Developer 2)</p>
    </div>
  );
}

/**
 * /dispatcher/dashboard — placeholder page (Task D1.2)
 * Stub created by Developer 1 for routing purposes only.
 * Owner of real content: Developer 2 (see D2.12 in the implementation plan).
 * Do not build Dispatcher page content here — that is Developer 2's task.
 */
export default function DispatcherDashboard() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Dispatcher Dashboard</h1>
      <p className="mt-2 text-gray-500">Route: /dispatcher/dashboard — placeholder (owned by Developer 2)</p>
    </div>
  );
}
